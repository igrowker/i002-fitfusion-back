import { Router } from "express";
const router = Router()
//En este archivo router.js se crean las funciones que van a estar guardadas en la carpeta routes
/* ej: 
router.post("/user/getUserById", (req, res) => {
    getUserByIdRoute(req, res) ==> Esta Funcion va a estar en un archivo getUserByIdRoute.js dentro de una
     carpeta con el mismo nombre, adentro de la carpeta routes
  }) 
     Luego de crear la respectiva ruta, falta la logica que va a estar almacenada en handlers.
*/
  
 





 export default router